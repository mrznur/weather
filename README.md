# React Weather App

Features

A simple Weather app where a user can see the current location's weather, can check the 7-day weather prediction.

User can also search for different places/cities and check its current weather.

The theme adjusts according to current weather. 

Built using React.js, WeatherAPI and TailwindCSS for styling.